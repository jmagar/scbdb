# Database connection (Docker Compose default)
DATABASE_URL=postgres://scbdb:scbdb@localhost:5432/scbdb

# LegiScan API key (required for regulatory tracking)
# Sign up at https://legiscan.com/legiscan to get a free API key
LEGISCAN_API_KEY=

# Rust log level (optional — defaults to info)
# Examples: debug, info, warn, error
# Per-crate: scbdb_scraper=debug,scbdb_legiscan=info
RUST_LOG=info
