# Optional: absolute or root-relative API base. Leave unset for same-origin.
# VITE_API_BASE_URL=https://scbdb.tootie.tv

# Vite dev/preview proxy target for /api when using same-origin frontend.
VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# VITE_API_KEY: Baked into the client bundle at build time — visible in browser DevTools.
# NEVER use a real production secret here. For production, use cookie-based auth or a BFF proxy.
VITE_API_KEY=
